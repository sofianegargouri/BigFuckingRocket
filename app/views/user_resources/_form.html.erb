<%= form_with(model: user_resource, local: true) do |form| %>
  <% if user_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_resource.errors.count, "error") %> prohibited this user_resource from being saved:</h2>

      <ul>
      <% user_resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :user_resource_quantity %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_resource_user_id %>
  </div>

  <div class="field">
    <%= form.label :resource_id %>
    <%= form.text_field :resource_id, id: :user_resource_resource_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
