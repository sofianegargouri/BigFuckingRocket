<%= form_with(model: part_cost, local: true) do |form| %>
  <% if part_cost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(part_cost.errors.count, "error") %> prohibited this part_cost from being saved:</h2>

      <ul>
      <% part_cost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cost %>
    <%= form.number_field :cost, id: :part_cost_cost %>
  </div>

  <div class="field">
    <%= form.label :belongs_to %>
    <%= form.text_field :belongs_to, id: :part_cost_belongs_to %>
  </div>

  <div class="field">
    <%= form.label :belongs_to %>
    <%= form.text_field :belongs_to, id: :part_cost_belongs_to %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
